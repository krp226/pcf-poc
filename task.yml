---
platform: linux

inputs:
  - name: code-repo

outputs:
  - name: generated-versions

params:
  SKIP_SSL_VALIDATION: false
  OPSMAN_URL:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:
  PIVNET_API_TOKEN:
  Endpoint:
  USERNAME:
  PASSWORD:
  ORG:
  SPACE:

run:
  path: /bin/bash
  args: ["./code-repo/upgrade-pipelines/tasks/get-versions/task.sh"]
